{
    "private": true,
    "name": "monorepo",
    "version": "1.0.0",
    "packageManager": "npm@10.0.0",
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "turbo": "latest"
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ]
}
