{
    "private": true,
    "name": "monorepo",
    "version": "1.0.0",
    "packageManager": "npm@10.0.0",
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "devDependencies": {
        "prettier": "latest",
        "turbo": "latest"
    },
    "prettier": {
        "tabWidth": 4,
        "singleQuote": true,
        "trailingComma": "all",
        "printWidth": 100
    },
    "workspaces": [
        "apps/*",
        "packages/*"
    ]
}
