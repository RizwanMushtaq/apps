# uv-demo-app

A tiny Python demo app managed with `uv`.

## What it does

Runs a simple script and prints:

`Hello from uv-demo-app!`

## Requirements

- Python 3.14+
- `uv`

## Run the app

From this folder, run:

```bash
uv run main.py
```
